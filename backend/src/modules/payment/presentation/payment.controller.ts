import { BadRequestException, Body, ConflictException, Controller, Headers, HttpCode, HttpStatus, NotFoundException, Param, Post, Req } from '@nestjs/common'; import { Capability } from '../../../shared/application/authorization-policy'; import type { AuthenticatedRequest } from '../../../shared/security/authenticated-principal'; import { BusinessAccess } from '../../../shared/security/security.decorators'; import { PaymentConflictError, PaymentInputError, PaymentNotFoundError, RegisterPaymentUseCase } from '../application/register-payment.use-case'; import type { Payment } from '../domain/payment'; import { RegisterPaymentRequestDto } from './dto/register-payment.request.dto';
@Controller('businesses/:businessId/bookings/:bookingId/payments') export class PaymentController { constructor(private readonly register: RegisterPaymentUseCase) {} @Post() @HttpCode(HttpStatus.CREATED) @BusinessAccess('businessId',Capability.PAYMENT_RECORD) async create(@Param('businessId') businessId:string,@Param('bookingId') bookingId:string,@Body() body:RegisterPaymentRequestDto,@Headers('idempotency-key') idempotencyKey:string|undefined,@Req() request:AuthenticatedRequest):Promise<Payment>{try{return await this.register.execute({...body,businessId,bookingId,idempotencyKey,actorUserId:request.authenticatedPrincipal.userId});}catch(error:unknown){if(error instanceof PaymentInputError)return new BadRequestException(error.message);if(error instanceof PaymentNotFoundError)return new NotFoundException(error.message);if(error instanceof PaymentConflictError||(error instanceof Error&&['OVERPAYMENT','IDEMPOTENCY_CONFLICT'].includes(error.message)))return new ConflictException(error.message);throw error;}}}
