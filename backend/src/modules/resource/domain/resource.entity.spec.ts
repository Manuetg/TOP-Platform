import { Resource } from './resource.entity'; import { ResourceStatus } from './resource-status.enum';
describe('Resource',()=>{it('expone todos sus getters',()=>{const createdAt=new Date('2026-01-01');const updatedAt=new Date('2026-02-02');const resource=Resource.create({id:'r',businessId:'b',name:'Cabaña 1',internalCode:'CAB-01',description:'Vista',capacityMinimum:1,capacityMaximum:4,capacityMaximumChildren:2,status:ResourceStatus.ACTIVE,sortOrder:3,createdAt,updatedAt});expect(resource.id).toBe('r');expect(resource.businessId).toBe('b');expect(resource.name).toBe('Cabaña 1');expect(resource.internalCode).toBe('CAB-01');expect(resource.description).toBe('Vista');expect(resource.capacityMinimum).toBe(1);expect(resource.capacityMaximum).toBe(4);expect(resource.capacityMaximumChildren).toBe(2);expect(resource.status).toBe(ResourceStatus.ACTIVE);expect(resource.sortOrder).toBe(3);expect(resource.createdAt).toBe(createdAt);expect(resource.updatedAt).toBe(updatedAt);});});
