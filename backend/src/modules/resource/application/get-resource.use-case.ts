import { Inject, Injectable } from '@nestjs/common'; import { BUSINESS_REPOSITORY, type BusinessRepository } from '../../business/business.contract'; import { RESOURCE_REPOSITORY, type ResourceRepository } from '../domain/resource.repository'; import { Resource } from '../domain/resource.entity';
export class InvalidBusinessIdError extends Error {} export class InvalidResourceIdError extends Error {} export class ResourceBusinessNotFoundError extends Error {} export class ResourceNotFoundError extends Error {} const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
@Injectable() export class GetResourceUseCase { constructor(@Inject(BUSINESS_REPOSITORY)private readonly businesses:BusinessRepository,@Inject(RESOURCE_REPOSITORY)private readonly resources:ResourceRepository){} async execute(businessId:string,resourceId:string):Promise<Resource>{if(!uuid.test(businessId))throw new InvalidBusinessIdError('El identificador del negocio no es válido.');if(!uuid.test(resourceId))throw new InvalidResourceIdError('El identificador del recurso no es válido.');if(!await this.businesses.findById(businessId))throw new ResourceBusinessNotFoundError('El negocio no existe.');const resource=await this.resources.findByIdAndBusinessId(resourceId,businessId);if(!resource)throw new ResourceNotFoundError('El recurso no existe.');return resource;} }
