services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: top
      POSTGRES_PASSWORD: top
      POSTGRES_DB: top
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
