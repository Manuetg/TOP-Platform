import type { INestApplication } from '@nestjs/common'; import { Test } from '@nestjs/testing'; import request from 'supertest'; import { AppModule } from '../../src/app.module'; import { configureApplication } from '../../src/config/configure-application'; import { Business } from '../../src/modules/business/domain/business.entity'; import { BusinessStatus } from '../../src/modules/business/domain/business-status.enum'; import { BUSINESS_REPOSITORY } from '../../src/modules/business/domain/business.repository'; import { RatePlan } from '../../src/modules/pricing/domain/rate-plan.entity'; import { RATE_PLAN_REPOSITORY } from '../../src/modules/pricing/domain/rate-plan.repository'; import { RatePlanStatus } from '../../src/modules/pricing/domain/rate-plan-status.enum'; import { RESOURCE_REPOSITORY } from '../../src/modules/resource/domain/resource.repository'; import { Resource } from '../../src/modules/resource/domain/resource.entity'; import { ResourceStatus } from '../../src/modules/resource/domain/resource-status.enum';
const businessId='11111111-1111-4111-8111-111111111111'; const resourceId='22222222-2222-4222-8222-222222222222'; const business=(status=BusinessStatus.ACTIVE)=>Business.create({id:businessId,businessNumber:null,name:'TOP',legalName:null,taxId:null,timezone:'America/Asuncion',currency:'PYG',status,createdAt:new Date(),updatedAt:new Date()}); const resource=(status=ResourceStatus.ACTIVE)=>Resource.create({id:resourceId,businessId,name:'Cabaña',internalCode:'CAB-1',description:null,capacityMinimum:1,capacityMaximum:2,capacityMaximumChildren:0,status,sortOrder:0,createdAt:new Date(),updatedAt:new Date()});
describe('Pricing endpoint',()=>{let app:INestApplication;let currentBusiness:Business|null;let currentResource:Resource|null;beforeAll(async()=>{const module=await Test.createTestingModule({imports:[AppModule]}).overrideProvider(BUSINESS_REPOSITORY).useValue({findById:()=>Promise.resolve(currentBusiness),create:jest.fn(),list:jest.fn(),update:jest.fn()}).overrideProvider(RESOURCE_REPOSITORY).useValue({findByIdAndBusinessId:()=>Promise.resolve(currentResource),findByBusinessAndCode:jest.fn(),listByBusinessId:jest.fn(),create:jest.fn(),update:jest.fn()}).overrideProvider(RATE_PLAN_REPOSITORY).useValue({create:(data:{businessId:string;name:string;description:string|null;baseNightlyAmountMinor:number;currency:string;validFrom:string|null;validTo:string|null;resourceIds:string[]})=>Promise.resolve(RatePlan.create({id:'33333333-3333-4333-8333-333333333333',...data,status:RatePlanStatus.ACTIVE,resources:data.resourceIds.map((id)=>({id,name:'Cabaña',internalCode:'CAB-1'})),createdAt:new Date(),updatedAt:new Date()}))}).compile();app=module.createNestApplication();configureApplication(app);await app.init();});afterAll(async()=>app.close());beforeEach(()=>{currentBusiness=business();currentResource=resource();});it('crea tarifa y expone DTO público',async()=>{await request(app.getHttpServer()).post(`/api/businesses/${businessId}/rate-plans`).send({name:' Estándar ',baseNightlyAmountMinor:450000,resourceIds:[resourceId]}).expect(201).expect(({body}:{body:Record<string,unknown>})=>{expect(body).toMatchObject({businessId,name:'Estándar',currency:'PYG',status:'ACTIVE',baseNightlyAmountMinor:450000});expect(body).not.toHaveProperty('props');});});it.each([{name:' ',baseNightlyAmountMinor:1,resourceIds:[]},{name:'Plan',baseNightlyAmountMinor:0,resourceIds:[]},{name:'Plan',baseNightlyAmountMinor:1,resourceIds:[resourceId,resourceId]}])('rechaza body inválido',async(body)=>{await request(app.getHttpServer()).post(`/api/businesses/${businessId}/rate-plans`).send(body).expect(400);});it('protege ausencias, archivados y tenant',async()=>{currentBusiness=null;await request(app.getHttpServer()).post(`/api/businesses/${businessId}/rate-plans`).send({name:'Plan',baseNightlyAmountMinor:1,resourceIds:[]}).expect(404);currentBusiness=business(BusinessStatus.ARCHIVED);await request(app.getHttpServer()).post(`/api/businesses/${businessId}/rate-plans`).send({name:'Plan',baseNightlyAmountMinor:1,resourceIds:[]}).expect(409);currentBusiness=business();currentResource=null;await request(app.getHttpServer()).post(`/api/businesses/${businessId}/rate-plans`).send({name:'Plan',baseNightlyAmountMinor:1,resourceIds:[resourceId]}).expect(404);currentResource=resource(ResourceStatus.ARCHIVED);await request(app.getHttpServer()).post(`/api/businesses/${businessId}/rate-plans`).send({name:'Plan',baseNightlyAmountMinor:1,resourceIds:[resourceId]}).expect(409);});});
