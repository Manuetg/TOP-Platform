import { PaymentStatus, type Payment, type PaymentRepository, type RegisterPaymentData } from '../../../src/modules/payment/domain/payment';
const payments: Payment[] = [];
export const paymentRepositoryFake: PaymentRepository = { register: (data: RegisterPaymentData, total: number) => { const previous = payments.find((item) => item.businessId === data.businessId && item.idempotencyKey === data.idempotencyKey); if (previous) { if (previous.requestFingerprint !== data.requestFingerprint) return Promise.reject(new Error('IDEMPOTENCY_CONFLICT')); return Promise.resolve({ payment: previous, duplicate: true }); } const paid = payments.filter((item) => item.bookingId === data.bookingId && item.status === PaymentStatus.RECORDED).reduce((sum, item) => sum + item.amountMinor, 0); if (paid + data.amountMinor > total) return Promise.reject(new Error('OVERPAYMENT')); const payment: Payment = { id: `p0000000-0000-4000-8000-${String(payments.length + 1).padStart(12, '0')}`, ...data, createdAt: new Date() }; payments.push(payment); return Promise.resolve({ payment, duplicate: false }); } };
export const snapshots = new Map<string, { id: string; businessId: string; bookingId: string; currency: string; totalAmountMinor: number; items: []; createdAt: Date }>();
export const pricingSnapshotRepositoryFake = { create: () => Promise.reject(new Error('No corresponde crear snapshots desde Payment.')), findByBookingId: (bookingId: string) => Promise.resolve(snapshots.get(bookingId) ?? null) };
export function resetPaymentFakes(): void { payments.length = 0; snapshots.clear(); }
export function paymentCount(): number { return payments.length; }
